#!/usr/bin/env groovy
//Leave the above line alone.  It identifies this as a groovy script.

//Modify the below parameters to match the values for this particular repo. The paths are relative to the repository directory. 
def viPath = "source\\add.vi"
def projPath = "source\\add-proj.lvproj"
def buildTarget = "My Computer"
def buildSpec = "exe"

//Leave the below line alone.  It pulls in the pipeline definition from the Global Library so that code is nut duplicated 
//in mmultiple locations and will be easy to modify when necessary. 
pipelineBuild(viPath, projPath, buildTarget, buildSpec)


